name: actions-jobs

on:
  ## scheduled job at 5:30 UTC every sunday
  # schedule:
  #  - cron: '30 5 * * 0'
  # on push
  push:
    branches: [ main ]
  # manual trigger
  workflow_dispatch:

permissions:
      id-token: write
      contents: read
      security-events: write

jobs:
  sec-scans:
    runs-on: ubuntu-latest

    steps:
    - name: Testing
      run: |
        echo "this is a test"
